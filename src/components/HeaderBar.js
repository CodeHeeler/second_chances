import React from 'react';

const HeaderBar = () => {

  const barStyle = {
    width: "100%",
    backgroundColor: "#000",
    padding: "0 auto"
  }

  return (
    <h1 style={barStyle}>
  </h1>
  )
}

export default MainLogo;
